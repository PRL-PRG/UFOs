[package]
name = "ufos_core"
version = "0.1.0"
authors = ["Colette 🌩︎‍💝 Kerr <Colette.m.y.Kerr@gmail.com>"]
edition = "2018"

# [features]
# low_level_ufos = []

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# userfaultfd = "0.2.0" # requires libclang1 installed
#userfaultfd = { git = "https://github.com/electroCutie/userfaultfd-rs", branch = "readwrite_fix" }
anyhow = "1.0.38"
blake3 = { version = "0.3.7", features = ["rayon"] }
crossbeam = "0.8.0"
libc = "0.2.80"
log = "0.4.14"
nix = "0.17"
num = "0.3.1" # for One
promissory = { git = "https://github.com/electroCutie/promissory", commit = "8f3fc2f2983f555de2019212671cc535a92e50a4" }
segment-map = "0.1.1" # for maps over ranges
thiserror = "1.0"
userfaultfd = { path = "/home/ckerr/workspace/userfaultfd-rs/" }

# stderrlog = "0.5.1"


# [build-dependencies]
# cbindgen = "0.18.0"

[build]
target_os = "linux"


[profile.release]
lto = true # better optimizations

